# Telegram Weather Bot Assignment

This is a fullstack monorepo that includes:

- A Telegram bot for weather updates (`/bot`)
- An admin panel API built with Express.js (`/backend`)
- A React.js admin panel UI (`/frontend`)
